#Define a function called hotel_host with one argument nights as input
def hotel_cost(nights):
    return 140*nights

#define a function called plane_ride_cost that takes a string, city, as input.
def plane_ride_cost(city):
if "Rome" == city:
    return 900